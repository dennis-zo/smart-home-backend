apiVersion: apps/v1
kind: Deployment
metadata:
  name: smart-home-deployment
  namespace: default
spec:
  replicas: 1 # שני מופעים לשרידות
  selector:
    matchLabels:
      app: smart-home
  template:
    metadata:
      labels:
        app: smart-home
    spec:
      containers:
        - name: smart-home-api
          image: denniszo/smart-home-backend:2.3.0
          imagePullPolicy: Always
          env:
            - name: HA_URL
              valueFrom:
                secretKeyRef:
                  name: smart-home-credentials
                  key: HA_URL
            - name: HA_TOKEN
              valueFrom:
                secretKeyRef:
                  name: smart-home-credentials
                  key: HA_TOKEN
            - name: TELEGRAM_TOKEN
              valueFrom:
                secretKeyRef:
                  name: smart-home-credentials
                  key: TELEGRAM_TOKEN
            - name: TELEGRAM_CHAT_ID
              valueFrom:
                secretKeyRef:
                  name: smart-home-credentials
                  key: TELEGRAM_CHAT_ID
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: smart-home-credentials
                  key: GEMINI_API_KEY
            - name: GEMINI_MODELS
              valueFrom:
                secretKeyRef:
                  name: smart-home-credentials
                  key: GEMINI_MODELS
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: smart-home-credentials
                  key: MONGO_URI
            - name: MONGO_DB_NAME
              valueFrom:
                secretKeyRef:
                  name: smart-home-credentials
                  key: MONGO_DB_NAME
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
